{
  "name": "good-otp-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:preview": "pnpm build && wrangler pages deploy dist --project-name=good-otp-web"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}